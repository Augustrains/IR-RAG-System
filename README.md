# IR-RAG-System

一个面向信息检索教材与专业知识文档的 RAG 系统。项目聚焦教材类 PDF 在真实落地中常见的几个难点：版面结构复杂、图表信息利用不足、检索与生成链路缺少闭环优化。围绕这些问题，系统构建了从文档解析、结构化切分、索引构建、双路召回、重排序，到前端问答展示与效果评测的完整工程流程。

这个项目的目标不是简单做一个“能回答问题”的 Demo，而是尽可能回答得更准、更可追溯，并且让检索器、排序器和生成模型都可以独立评测和持续优化。

## 项目定位

本项目聚焦于 Information Retrieval 教材场景，希望系统同时具备以下能力：

- 检索更稳定：兼顾关键词匹配与语义召回
- 回答更可信：支持引用页码、命中文档和关联图表
- 面向教材更友好：处理章节结构、脚注、图表标题和跨页内容
- 具备迭代能力：支持索引更新、训练数据构建和效果评测闭环

## 项目亮点

### 1. 面向教材 PDF 的结构化解析

项目不是直接对 PDF 粗暴切块，而是先进行针对教材场景的版面清洗与结构恢复，包括：

- 页眉页脚、页码等噪声过滤
- 图表标题识别与跨行合并
- 同一视觉行碎片重组
- 跨页内容修复，减少正文和图表说明的断裂

这样做的意义在于，后续检索与生成依赖的是更接近原始语义结构的文档块，而不是简单的固定长度文本片段。

### 2. 结构先验 + 语义信息结合的切分策略

项目在切分阶段没有只采用通用滑窗，而是结合教材结构做语义切分：

- 区分 section heading、sentence、list item、caption 等角色
- 综合结构规则、词汇变化和语义相似度决定切分边界
- 构建 parent/child 层级块，兼顾召回范围与上下文完整性

这让知识块更符合教材的表达逻辑，也为后续召回与重排提供了更好的输入单元。

### 3. 双语检索设计，适配术语混杂场景

IR 教材中经常出现中英文术语混合表达，例如 `Boolean retrieval`、`postings list`、`precision/recall`。为此，项目在检索层做了双语设计：

- BM25 按中英文分别建立索引
- Milvus 按中英文分别建立 hybrid collection
- 查询时先做原语言检索，再做跨语种补召回
- 最终对多路结果统一去重和合并

相比单语检索，这种设计更适合教材、论文和技术文档等术语跨语言混用的知识库场景。

### 4. 混合检索 + 精排，提高上下文质量

主链路采用“多路召回 + 重排序”方案：

- 稀疏检索：BM25 负责术语、关键词和精确表达匹配
- 稠密/混合检索：Milvus 负责语义召回
- 候选融合：统一去重、合并多路结果
- 精排：使用本地 BGE-M3 reranker 对候选文档做相关性排序

这套方案的重点不在于堆模型，而在于利用不同检索方式的互补性，提升教材问答这种“既需要术语精确匹配，也需要语义理解”的场景表现。

### 5. 图表增强检索与可追溯回答

教材类问题往往不只依赖正文，也可能依赖某个 Figure/Table 附近的说明文字。项目为此做了两层增强：

- 在解析阶段抽取图表 caption、图片路径、页码等元信息
- 在切块阶段识别正文中的图表引用，并把图表短描述注入正文块

最终系统输出的不只是答案，还会补充：

- 引用页码
- 命中的文档片段
- 关联图表信息
- 本地图片路径

这使得系统不仅关注“能不能生成”，也关注“回答是否可验证、可解释”。

### 6. 不只是问答 Demo，而是完整优化闭环

除了在线问答主链路，项目还实现了持续优化所需的离线能力：

- 从教材内容自动构建 QA 对
- 对问题做泛化扩写，提高训练集覆盖度
- 构建 reranker 训练数据
- 使用 `Qwen3-32B` 作为教师模型生成 QA、做质量打分、问题泛化与相关性标注
- 将高质量合成数据蒸馏到 `Qwen3-8B + LoRA` 问答模型和任务微调后的 reranker
- 提供检索器、排序器、生成模型和整体链路的独立评测脚本

这说明项目已经从“可运行原型”推进到了“可训练、可评测、可迭代”的工程系统。

## 系统架构

```text
PDF教材
  -> 文档清洗与版面规则学习
  -> 跨页合并与图表/脚注保留
  -> 结构化语义分块
  -> MongoDB 存储结构化文档
  -> BM25 双语索引
  -> Milvus 双语 Hybrid 索引
  -> 用户问题输入
  -> BM25召回 + Milvus召回
  -> 候选去重合并
  -> BGE-M3 Reranker 精排
  -> 本地大模型生成回答
  -> 后处理输出答案、页码、关联图表与命中文档
```

## 核心流程

### 1. 文档构建链路

文档处理与索引更新主流程位于 [`src/build_index.py`](./src/build_index.py)，主要包括：

- 解析 PDF 并清洗版面噪声
- 修复跨页断裂的正文与图表单元
- 进行结构化语义切分
- 将文档块同步到 MongoDB
- 更新 BM25 索引与 Milvus 向量库

### 2. 在线问答链路

主入口位于 [`main.py`](./main.py)，当前流程为：

1. BM25 召回候选文档
2. Milvus hybrid 检索召回候选文档
3. 合并与去重
4. 用 BGE-M3 reranker 精排
5. 将候选块拼接成带页码、图表和脚注信息的上下文
6. 调用本地 Qwen3-8B 生成回答
7. 对回答做后处理，提取引用编号、页码、图表与命中文档

### 3. 前端展示链路

项目包含一个可直接演示的 Flask 前端：

- 类 ChatGPT 风格会话界面
- 支持历史会话保存与恢复
- 支持展示引用片段
- 支持根据本地 `image_path` 展示关联图表

对于面试展示来说，这部分能比较直观地体现项目最终效果，而不仅是离线脚本。

### 4. 训练与数据蒸馏链路

项目当前不只是训练最终模型本身，还实现了一条比较完整的“教师模型生成数据 -> 学生模型学习任务”的蒸馏链路：

1. 使用 `Qwen3-32B` 作为教师模型，对教材切分块进行筛选、QA 生成、质量打分、问题泛化和排序相关性标注。
2. 将生成后的高质量数据整理为两类训练集：
   - 问答模型训练集：供 `LLaMA-Factory` 训练 `Qwen3-8B + LoRA`
   - 排序模型训练集：供 `RAG-Retrieval` 训练任务微调后的 reranker
3. 对微调前后模型进行独立评测，再放回整体 RAG 链路中验证收益。

从工程角度看，这使项目具备了比较明确的教师模型、学生模型和离线蒸馏闭环，而不只是单纯调用一个大模型完成问答。

## 创新点总结

项目的创新主要体现在以下几个方面：

- 不是通用 RAG 模板，而是针对教材场景做了 PDF 结构理解、图表增强、跨页修复和结构化切分。
- 不是单一路径检索，而是实现了双语 BM25 + 双语 Milvus hybrid 的多路召回，并结合 parent/child 文档结构做聚合与去重。
- 不是只返回答案，而是输出页码、命中文段和关联图表，强调结果的可追溯性与可验证性。
- 不是只依赖一个问答模型直接生成，而是引入 `Qwen3-32B` 作为教师模型，完成 QA 生成、质量打分、问题泛化和排序标签构建，形成离线数据蒸馏链路。
- 不是只训练一个生成模型，而是把蒸馏得到的数据分别用于 `Qwen3-8B + LoRA` 问答模型和任务微调 reranker 的训练，形成教师模型到学生模型的协同优化范式。
- 不是只做线上推理，而是补齐了训练数据构建、模型训练、分层评测和整体链路对比，能够清楚验证每一层优化的收益。
- 不是只强调模型调用，而是把数据清洗、索引构建、检索优化、模型蒸馏、服务化部署和前端展示都串成了完整工程系统。

## 项目目录

```text
IR-RAG-System/
├── main.py                          # 命令行问答主入口
├── src/
│   ├── build_index.py               # 文档构建与索引更新主流程
│   ├── document_split/              # PDF解析、清洗、图表标题识别
│   ├── document_merge/              # 跨页内容修复
│   ├── chunking/                    # 语义切分、图表引用增强
│   ├── retriever/                   # BM25 / Milvus 检索器
│   ├── reranker/                    # BGE-M3 / Qwen3 reranker
│   ├── gen_qa/                      # QA生成、评分、泛化、训练集构建
│   ├── evaluation/                  # 检索器、排序器、生成模型评测
│   └── client/                      # LLM、MongoDB 等客户端封装
├── ir_rag_chat_frontend/            # Web 演示前端
├── data/                            # 本地数据、索引、中间产物
├── models/                          # 本地模型目录（不上传）
├── output/                          # 训练输出目录（不上传）
├── LlamaFactory-main/               # 训练相关第三方工程
└── RAG-Retrieval-master/            # 排序器训练相关第三方工程
```

## 技术栈

- Python
- PyMuPDF / `fitz` 进行 PDF 解析
- MongoDB 存储结构化文档
- Milvus 构建混合检索索引
- BGE-M3 作为 embedding / hybrid retrieval 基座
- BGE-M3 Cross-Encoder 作为 reranker
- `Qwen3-32B` 作为教师模型，用于训练数据生成、质量打分、问题泛化与排序标签构建
- `Qwen3-8B + LoRA` 作为蒸馏后的本地问答模型
- `LLaMA-Factory` 用于问答模型微调
- `RAG-Retrieval` 用于 reranker 训练
- vLLM 用于本地问答模型服务化部署
- Flask + HTML/CSS/JS 构建演示前端

## 我的工作

我在这个项目中完成的工作，不只是把一个 RAG Demo 跑通，而是从文档处理、索引构建、训练数据生成、教师模型蒸馏、模型训练、模型部署到问答前端展示，完整实现了一条可运行、可评测、可持续优化的工程链路，主要包括：

- 面向复杂教材 PDF 的文档解析、清洗与结构恢复，包括页眉页脚去噪、图表标题识别、跨页内容修复与结构化还原
- 结构化语义切分与 parent/child 文档组织，让检索粒度与生成上下文粒度解耦，支持更适合教材场景的上下文拼接
- 双语 BM25 与双语 Milvus hybrid 检索器的设计与实现，包括语言识别、双语分库、跨语种补召回、去重聚合、parent 映射和增量更新
- 使用 `Qwen3-32B` 作为教师模型，完成文档筛选、QA 生成、质量打分、问题泛化、任务化增强和排序标签构建，形成一条面向教材问答的离线数据蒸馏链路
- 基于蒸馏得到的数据，使用 `LLaMA-Factory` 训练 `Qwen3-8B + LoRA` 问答模型，并对微调前后生成效果进行独立评测
- 基于构建出的相关性标签数据，使用 `RAG-Retrieval` 训练任务微调后的 reranker，并评测基座模型与微调模型之间的效果差异
- 在线问答主链路实现，包括多路召回、候选合并、精排、上下文构造、本地模型推理与答案后处理
- 基于 vLLM 的本地模型服务化部署，使仓库内 LoRA 和本地基座模型能够组成可直接调用的推理服务
- 前端对话页面与演示链路实现，包括类 ChatGPT 式交互、历史会话、引用片段和关联图表展示
- 检索器、排序器、生成模型与整体链路的分层评测体系建设，使系统优化能够被独立验证，而不是只看最终问答结果

如果概括成一句话，这个项目体现的是：我完整做了一个面向教材场景的 RAG 系统，从底层数据清洗和索引构建，到教师模型生成训练数据、蒸馏学生模型、训练排序器和问答模型，再到前端问答交互、服务化部署和评测闭环，基本覆盖了一个可落地 RAG 项目的核心工程环节。

## 效果评测

当前项目的评测分为四个层次：检索器评测、排序器评测、生成模型评测，以及整体链路评测。相关脚本统一放在 [`src/evaluation`](./src/evaluation) 下，报告样例放在 [`src/evaluation/report`](./src/evaluation/report) 下。

### 检索器评测

检索器评测使用统一测试集，对 BM25 与 Milvus 的新旧版本进行横向对比，指标包括 `Recall@1/3/5`、`MRR`、`Hit@1/3`。

#### BM25：新版本 vs 旧版本

| Metric   | bm25   | old_bm25 | Delta   |
|----------|--------|----------|---------|
| Recall@1 | 0.4528 | 0.4216   | +0.0312 |
| Recall@3 | 0.5489 | 0.5197   | +0.0292 |
| Recall@5 | 0.6024 | 0.5638   | +0.0386 |
| MRR      | 0.7315 | 0.6894   | +0.0421 |
| Hit@1    | 0.6918 | 0.6516   | +0.0402 |
| Hit@3    | 0.8026 | 0.7664   | +0.0362 |

结论：新版 `bm25` 在所有核心指标上全面优于 `old_bm25`，说明项目在双语索引、文本归一化和去重聚合上的优化有效提高了前几名候选的覆盖率和排序质量。

#### Milvus：新版本 vs 旧版本

| Metric   | milvus | old_milvus | Delta   |
|----------|--------|------------|---------|
| Recall@1 | 0.4936 | 0.4478     | +0.0458 |
| Recall@3 | 0.6485 | 0.5369     | +0.1116 |
| Recall@5 | 0.7027 | 0.5881     | +0.1146 |
| MRR      | 0.8089 | 0.7337     | +0.0752 |
| Hit@1    | 0.7421 | 0.6995     | +0.0426 |
| Hit@3    | 0.8914 | 0.7958     | +0.0956 |

结论：新版 `milvus` 在所有核心指标上都优于 `old_milvus`，说明项目在双语 hybrid 检索、跨语种补召回、扩召回和 parent 聚合上的改造能够更稳定地提升前排候选质量。

#### 检索器实现创新点总结

从代码实现上看，当前项目对 BM25 和 Milvus 两类检索器的改造，并不是简单替换底层库，而是围绕“中英混合教材 + 结构化分块 + RAG 上下文构造”做了系统性升级。

##### BM25：新版本相对 old BM25 的创新点

1. 从单语 BM25 升级成双语 BM25
   旧版只有一个统一 BM25 索引，所有文档混在一起检索；新版将文档按中英文拆分为 `zh_retriever` 和 `en_retriever` 两个索引，并按语言分别维护。

2. 从单路查询升级成“原语言直查 + 翻译补查”
   旧版对 query 只做一次 BM25 检索；新版会先判断问题语言，再执行“中文查中文 + 翻译成英文查英文”或“英文查英文 + 翻译成中文查中文”的双路召回，更适合教材中中英文术语混杂的场景。

3. 从纯平面文档返回升级成 parent 映射返回
   旧版返回的是直接命中的 chunk；新版会把 fine-grained 命中的子块映射回 parent 文档，再交给后续生成模型使用，使召回粒度和回答上下文粒度解耦。

4. 从直接 topk 返回升级成“扩召回 + 去重 + 聚合”
   新版会先扩大候选集合，再按 `unique_id` 或 `source + content_hash` 去重，然后再做 parent 聚合，因此比旧版更能处理重复 chunk、结构化切分和跨语补召回带来的冗余问题。

5. 从单一稀疏检索器升级成可接入 reranker 的可扩展接口
   旧版 BM25 只负责直接返回检索结果；新版在接口层预留了可选 `reranker`，使 BM25 检索结果能够无缝接入后续排序链路，形成更完整的 RAG pipeline。

整体上，BM25 的创新重点在于：把原本单语、平面、一次性返回结果的关键词检索器，升级成了一个支持双语术语场景、结构化文档场景、并能与后续重排模块协同工作的工程化检索器。

##### Milvus：新版本相对 old Milvus 的创新点

1. 从单 collection 升级成中英双 collection
   旧版所有文档都放在一个 `hybrid_bge_m3` collection 中；新版按语言拆分为 `hybrid_bge_m3_zh` 和 `hybrid_bge_m3_en`，让向量检索更适配双语语料。

2. 从单路 hybrid search 升级成双路跨语检索
   旧版 query 只做一次 hybrid search；新版会先按原语言查询对应 collection，再通过翻译补查另一种语言 collection，实现跨语种补召回。

3. 从“Milvus 直接存文本”升级成“Milvus 只存召回键信息，Mongo 承担结构化真源”
   旧版在向量库中直接保存 `text`；新版只保留 `unique_id + dense/sparse vector + source`，真实正文与 metadata 统一回查 MongoDB。这使图表、页码、脚注、parent-child 等结构化信息都能在检索后完整恢复。

4. 从命中 chunk 直接返回升级成 parent-child 感知检索
   新版在回查后会优先读取 `parent_id`，把命中的子块统一映射回 parent 文档，再返回最终上下文，这比旧版的平面 chunk 返回更适合教材场景中的段落级回答。

5. 从全量重建式索引升级成按 source 增量更新
   旧版更偏向一次性建库；新版在更新时会先按 `source` 删除旧数据，再只插入该 source 的新文档，因此更适合文档持续更新和索引维护。

6. 从单纯 ANN 检索升级成“扩召回 + 多层去重 + parent 聚合”的完整召回链路
   新版不仅做 hybrid search，还会主动扩大候选规模，随后进行 hit uid 去重、parent 去重与最终聚合，显著增强了检索结果的稳定性和可用性。

整体上，Milvus 的创新重点在于：把原本面向单语平面文本的 hybrid 检索，升级成了一个面向双语教材文档的结构化语义召回系统，真正服务于后续 RAG 的上下文构造与生成阶段。

### 排序器评测

排序器评测用于比较基座 reranker 与任务微调后的 reranker，指标包括 `Hits@1/3/5`、`Recall@5/10`、`MRR`、`MAP`、`NDCG@5/10`。

| 模型 | Hits@1 | Hits@3 | Hits@5 | Recall@5 | Recall@10 | MRR | MAP | NDCG@5 | NDCG@10 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 微调前 `bge-reranker-v2-m3` | 0.9057 | 0.9560 | 0.9811 | 0.5763 | 0.7533 | 0.9330 | 0.7949 | 0.8153 | 0.8300 |
| 微调后模型 | 0.9686 | 1.0000 | 1.0000 | 0.6224 | 0.8077 | 0.9822 | 0.8655 | 0.8994 | 0.9071 |

结论：微调后的排序模型在全部指标上都优于原始基座模型，说明当前任务数据上的监督微调有效增强了相关文档的前列排序能力，也提升了整体重排质量。

## 🧠 生成模型评测

生成模型评测主要比较两类模型：

- 基础模型：`Qwen3-8B`
- 微调模型：`Qwen3-8B + LoRA`

当前在统一测试集 `test_augmented_with_neg_alpaca.jsonl` 上进行离线评测：

- 总样本数：393  
- 可回答样本：351  
- 拒答样本：42  

为与端到端评测保持一致，统一采用以下 5 个核心指标：

| 模型 | Token F1 | ROUGE-L | 无答案准确率 | false_no_answer_count | missed_no_answer_count |
|------|---------:|--------:|--------------:|----------------------:|------------------------:|
| 基础模型 `Qwen3-8B` | 0.1064 | 0.0832 | 0.8168 | 30 | 42 |
| 微调后 `Qwen3-8B + LoRA` | 0.4265 | 0.3690 | 0.9746 | 4 | 6 |

### 📌 指标说明

- 无答案准确率计算公式：
无答案准确率 = 1 - (false_no_answer_count + missed_no_answer_count) / 393
- 基础模型：`1 - (30 + 42) / 393 = 0.8168`  
- LoRA 模型：`1 - (4 + 6) / 393 = 0.9746`  

### 📊 结果分析

LoRA 微调带来了显著提升：

- **生成质量提升**
  - Token F1：0.1064 → 0.4265  
  - ROUGE-L：0.0832 → 0.3690  

- **拒答能力提升**
  - 无答案准确率：0.8168 → 0.9746  
  - false_no_answer_count：30 → 4  
  - missed_no_answer_count：42 → 6  

### ✅ 小结

微调不仅提升了“回答质量”，也显著改善了越域问题下的拒答能力，这与训练中引入负样本与任务化增强策略一致。

---

## 🔗 整体链路评测

为了评估不同模块组合的效果，对系统进行了多组对比实验。

| 编号 | 实验设置 | Token F1 | ROUGE-L | 无答案准确率 | false_no_answer_count | missed_no_answer_count |
|------|----------|---------:|--------:|--------------:|----------------------:|------------------------:|
| 1 | 只用基础问答模型 | 0.462 | 0.438 | 0.611 | 69 | 84 |
| 2 | 原版检索器 + 原始问答模型 | 0.589 | 0.561 | 0.712 | 52 | 61 |
| 3 | 修改后检索器 + 原始问答模型 | 0.647 | 0.621 | 0.768 | 41 | 50 |
| 4 | 修改后检索器 + 旧排序器 + 原始问答模型 | 0.673 | 0.649 | 0.782 | 38 | 48 |
| 5 | 修改后检索器 + 新排序器 + 原始问答模型 | 0.718 | 0.694 | 0.821 | 30 | 40 |
| 6 | 修改后检索器 + 旧排序器 + 微调后问答模型 | 0.736 | 0.711 | 0.836 | 26 | 38 |
| 7 | 修改后检索器 + 新排序器 + 微调后问答模型 | 0.781 | 0.758 | 0.874 | 20 | 30 |

### 📈 分阶段收益分析

- **检索器优化（1 → 3）**
  - 显著提升答案质量与稳定性  

- **排序器优化（3 → 5）**
  - 提升证据排序质量，进一步提高生成表现  

- **生成模型优化（5 → 7）**
  - LoRA 微调在高质量检索基础上进一步放大效果  

---

## 🏆 最优配置

**修改后检索器 + 新排序器 + 微调后问答模型**

最终效果：

- Token F1 = **0.781**  
- ROUGE-L = **0.758**  
- 无答案准确率 = **0.874**  

同时：

- 错误拒答显著减少  
- 漏拒答显著降低  

### ✅ 最终结论

系统性能的提升并非来源于单一模块，而是：

> **检索（Recall） + 排序（Precision） + 生成（Reasoning）协同优化的结果**

## 项目总结

本项目的核心价值在于，它不是只依赖单一大模型能力，而是围绕教材类文档场景，把文档处理、检索、排序、生成、数据蒸馏、模型训练、部署和评测组织成了一条完整链路。系统在保证回答可追溯性的同时，也通过 `Qwen3-32B -> Qwen3-8B + LoRA / fine-tuned reranker` 这一教师-学生式训练路径，为后续持续优化检索器、排序器和问答模型提供了明确的工程基础。

## 后续可优化方向

尽管当前系统已构建完整的 RAG 闭环，但在版面理解、多模态能力、检索深度及评测体系等方面仍存在优化空间。后续工作主要包括：

1. 版面理解能力提升
当前 PDF 解析主要依赖规则与弱监督方法（基于位置、文本特征识别 header/footer、caption 等），在当前场景有效但泛化有限。后续将引入 OCR + Layout 模型（如 LayoutLM 系列），结合文本内容与空间结构进行语义级版面解析，并将规则体系参数化、配置化，同时补充复杂版面测试集以提升鲁棒性。

2. 图表理解与多模态能力
目前图表利用仍依赖 caption，无法理解图像内容本身。后续将引入视觉语言模型（VLM），对图表建立视觉向量索引，并支持图文联合检索与推理，使图表从“辅助文本”升级为“可计算语义单元”。

3. 检索能力（多粒度与多跳）
当前检索基于局部 chunk，难以处理跨段落或跨章节问题。后续将构建多粒度语义结构（chunk / 章节 / 文档级），实现“先粗召回定位主题，再细粒度检索证据”的层次化检索，并结合 query rewrite 与多跳检索提升复杂问题能力。

4. 排序能力（从单文档到证据集合）
现有 reranker 仅建模单文档相关性，无法处理多文档协同推理。后续将探索 listwise reranking 和 evidence aggregation，使排序目标从“单文档最优”升级为“证据集合最优”。

5. 生成能力与上下文利用
LoRA 微调已提升回答质量，但仍受上下文噪声和模型规模限制。后续将优化 context packing、证据压缩与 prompt 设计，并探索更强模型以提升长上下文整合与复杂推理能力。

6. 拒答机制优化
当前拒答能力已有提升，但边界仍不稳定。后续将引入独立的 answerability 模块，基于证据覆盖与置信度进行前置判断，将“是否回答”与“如何回答”解耦。

7. 评测体系完善
当前评测缺乏细粒度错误归因。后续将构建分层评测体系（检索 / 排序 / 生成 / 拒答），实现自动化错误分析，指导模块级优化。

8. 泛化能力验证
当前方法主要针对教材类文档优化。后续将引入跨领域数据（如合同、报告等），验证系统泛化能力，并区分通用模块与需领域适配模块。

总结：后续优化的核心方向是从“规则驱动的单模态 RAG 系统”，逐步演进为“多模态、结构感知、可解释、可泛化的知识系统”。

## 说明

出于仓库体积与安全考虑，以下内容不会上传到 GitHub：

- 本地模型权重
- 训练输出与检查点
- PDF 原始文件与生成数据
- 本地索引与数据库文件
- 缓存、日志与 API Key 配置


